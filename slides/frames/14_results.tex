\begin{frame}{Two-Phase Closes the Gap}
  \centering
  \input{figures/bars_final.tex}
  \vspace{0.2em}
  {\footnotesize Average gap to full scan: \alert{\GapTone\,pp $\to$ \GapTP\,pp} \quad(s1196 / s5378 even exceed it)}
  \note{Stacked per circuit: blue is one-frame coverage, orange is what two-phase recovers, and the thin grey sliver on top is the remaining gap to full scan. The grey is tiny everywhere, so the pipeline reaches close to the full-scan ceiling; the average gap falls from 31.26 to 4.24 points. s1196 and s5378 have no grey because they even exceed full-scan coverage.}
\end{frame}
